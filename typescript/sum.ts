
/**
 * 
 * @param num 
 * @returns 
 */
function toString(num){
    const message = 'thillai';
    let maybe: number = null;
    if (num) return String(maybe);

    const names = ["Alice", "Bob", "Eve"];
    names.forEach(function (s){
        console.log(s.toUpperCase());
    })
}
