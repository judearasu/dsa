function longestPalindromicSubstring(string) {
    let palindromeStr = "";
    let left = 0;
    let right = string.length -1;
    while(left < right){
        if(string[left] === string[right]){
            str
        }
    }
    // Write your code here.
}

console.log(longestPalindromicSubstring(string = "abaxyzzyxf"))